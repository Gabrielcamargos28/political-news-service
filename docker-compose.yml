services:
  db:
    image: postgres:15-alpine
    container_name: political_news_db
    environment:
      POSTGRES_DB: political_news
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
